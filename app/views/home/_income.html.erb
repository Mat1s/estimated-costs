<%= income.user_id %>|
<%= income.sum %>|
<%= income.category_id %>
<%= link_to "Edit", edit_income_path(income.id) %>
<%= link_to "delete", income_path(income.id), method: :delete, data: {confirm: "Are you sure?" } %>
<hr>